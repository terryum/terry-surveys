# Compatibility pointer: survey v2 orchestration

This filename remains for older callers. Use `orchestration-v2.md` and
`role-contracts-v2.md`. The repository `survey_harness` state is authoritative;
Claude `TeamCreate`/`TaskCreate` and Codex collaboration calls are dispatch
adapters, not the state store.
