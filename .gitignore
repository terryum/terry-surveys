.wrangler/
_workspace/
__pycache__/
*.pyc
.DS_Store
node_modules/
_refs_extracted.json
.claude/scheduled_tasks.lock

# Private survey content lives in the companion `terry-private` repo and is
# linked in via scripts/link-private.sh. The symlink itself is ignored so the
# restricted content never lands in the public monorepo.
surveys/snu-tactile-hand
revise-source/
**/revise-source/
_revise-source/
**/_revise-source/
